# SMART on FHIR Sandbox – example configuration
# Copy this file to .env and adjust values for your environment.

# Allowed origins for browser calls to the API (comma-separated).
CORS_ALLOW_ORIGINS=*

# External issuer URL advertised in metadata (matches the API host when deployed).
OIDC_ISSUER=http://localhost:3000/oauth2

# Secret used to sign OIDC cookies (override per environment).
OIDC_COOKIE_KEY=smart-sandbox-secret

# Absolute or relative path for the shared SQLite database.
SANDBOX_DB_PATH=data/storage/sandbox.sqlite

# Optional shared secret required to access admin APIs (e.g., /admin/tokens).
# Leave commented to disable admin routes.
# SANDBOX_ADMIN_TOKEN=change-me
